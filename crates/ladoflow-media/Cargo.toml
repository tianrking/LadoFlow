[package]
name = "ladoflow-media"
description = "Codec-neutral media metadata and deterministic frame scheduling for LadoFlow"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/tianrking/LadoFlow"
authors = ["LadoFlow contributors"]
publish = false

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"

# Keep this crate directly testable until the repository workspace manifest is
# updated by its owner.
[workspace]
